import "./globals.css";

export const metadata = { title: "QA Agent — Reports" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="ko"><body>{children}</body></html>;
}
